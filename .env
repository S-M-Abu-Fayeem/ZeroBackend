# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=zero_db
DB_USER=zero_user
DB_PASSWORD=zero_pass000

# Connection pool sizing
DB_MIN_CONN=2
DB_MAX_CONN=10

# Rerun Migrations (make it true in the first run)
RERUN_MIGRATIONS=false